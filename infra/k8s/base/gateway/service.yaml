apiVersion: v1
kind: Service
metadata:
  name: gateway
  labels:
    app.kubernetes.io/name: gateway
    app.kubernetes.io/part-of: aiops
spec:
  selector:
    app.kubernetes.io/name: gateway
  ports:
    # Port 3000 cluster-wide, matching docker-compose, so SERVICE_URL env vars
    # are identical locally and on EKS.
    - name: http
      port: 3000
      targetPort: http
