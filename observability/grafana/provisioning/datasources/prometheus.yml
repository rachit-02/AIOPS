apiVersion: 1
datasources:
  - name: Prometheus
    uid: prometheus        # fixed uid so dashboard JSON can reference it
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
